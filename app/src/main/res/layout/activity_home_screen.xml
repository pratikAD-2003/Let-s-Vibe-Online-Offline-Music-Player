<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bLight_4"
    tools:context=".Home.HomeScreen"
    tools:openDrawer="start">

    <include layout="@layout/home_screen_content" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationViewDrawer"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/bLight_6"
        app:drawerLayoutCornerSize="10dp"
        app:itemIconPadding="15dp"
        app:itemIconTint="@color/white"
        app:itemTextAppearance="@style/NavigationItemsTextStyle"
        app:itemTextColor="@color/white"
        app:menu="@menu/nav_items" />


</androidx.drawerlayout.widget.DrawerLayout>